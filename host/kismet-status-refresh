#!/bin/sh
set -eu

PID_FILE=${PID_FILE:-/run/waveshare-status.pid}

if [ ! -r "$PID_FILE" ]; then
        exit 0
fi

pid=$(cat "$PID_FILE" 2>/dev/null || true)
case "$pid" in
        ''|*[!0-9]*) exit 0 ;;
esac

kill -USR1 "$pid" 2>/dev/null || true
